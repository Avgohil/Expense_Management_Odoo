import React from 'react'

export default function Debug(){
  return (
    <div style={{padding:40}}>
      <h2>Debug page — React is rendering correctly.</h2>
      <p>If you see this, the router and React are working. Next check the browser console for errors on the /login page.</p>
    </div>
  )
}
